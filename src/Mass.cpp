#include "Mass.h"

// ======================== CONSTRUCTORS ============================//
Mass::Mass(float m, Vec3f pos) {
  mass = m;
  position = pos;
  velocity = Vec3f(0,0,0);
  force = Vec3f(0,0,0);
}

// ==========================================================================//

// ========================= OPERATORS ======================================//

// ==========================================================================//
